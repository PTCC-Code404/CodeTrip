@model CodeTrip.Models.Transporte;

<h1>Editar Transporte</h1>

<h4>Transporte</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="EditarTransporte" method="post">
            @*Esse token desempenha um papel crucial na prevenção de ataques de Cross-Site Request Forgery (CSRF).
            Um ataque CSRF ocorre quando um site malicioso, um e-mail, um programa de mensagens 
            instantâneas ou um programa induz um navegador da web de um usuário conectado a um aplicativo da web a executar uma 
            ação indesejada naquele aplicativo. Isso pode acontecer sem o conhecimento ou consentimento do usuário.*@
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <input type="hidden" asp-for="Id_Transp" class="form-control" />
                <span asp-validation-for="Id_Transp" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Tipo_Transp" class="control-label"></label>
                <input asp-for="Tipo_Transp" class="form-control" />
                <span asp-validation-for="Tipo_Transp" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label for="UF_Estado">Estado:</label>
                <select name="UF_Estado" id="UF_Estado" class="form-control" asp-items="@ViewBag.Estados">
                    <option value="">-- Selecione um Estado --</option>
                </select>
            </div>

            <div class="form-group">
                <input type="submit" value="Alterar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <div class="voltar-prod">
        <a asp-action="Index" class="btn btn-outline-dark">Voltar para Transporte</a>
    </div>
    <div class="voltar-prod">
        <a asp-area="" asp-controller="Home" asp-action="MenuSistema" class="btn btn-outline-dark">Voltar ao Menu Principal</a>
    </div>
</div>
